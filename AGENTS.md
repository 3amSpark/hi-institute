# Repository Instructions

- Do not run site build commands for verification, including `npm run build`, `astro build`, `pnpm build`, `yarn build`, or `bun run build`.
- Do not run routine verification checks, including type checking, linting, formatting checks, or tests, unless the user explicitly asks for them.
- Only run a site build if the user explicitly asks for a build in that request.
